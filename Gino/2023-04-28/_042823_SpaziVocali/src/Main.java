import java.util.Scanner;

public class Main {
    public static void main(String[] args)
    {
        Scanner input = new Scanner(System.in);
        String word = " ";
        long count = 0;

        System.out.print("| Insert phrase : ");
        word = input.nextLine();
        
        for(int i = 0; i < word.length(); i++)
        {
            if(word.substring(i, i + 1) == " " && )
        }

        System.out.print("The total amount of spaces followed by a vowel, is : " + count);
    }
}